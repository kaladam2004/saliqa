{
  "name": "warehouse-wms",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n \"API,UI\" -c \"cyan,magenta\" \"npm run dev:api\" \"npm run dev:ui\"",
    "dev:api": "cd warehouse-backend-node && npm run start:dev",
    "dev:ui": "cd warehouse-ui && npm run dev",
    "install:all": "npm install --prefix warehouse-backend-node && npm install --prefix warehouse-ui",
    "build": "npm run build --prefix warehouse-backend-node && npm run build --prefix warehouse-ui"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
